# lab

개인 실험/학습용 스크래치 저장소. 프로덕션 코드가 아님.

## 기본 규칙

- 최상위 폴더 하나 = 독립 실험. 폴더 간 import 금지, 공통 모듈 추출 금지.
- 기본값은 "버려도 되는 코드". 테스트/추상화/예외처리는 명시적 요청 시에만.
- 전역 CLAUDE.md의 테스트 선행 규칙은 여기서 적용하지 않음.
  동작 확인은 스크립트 직접 실행으로 충분.
- 실험이 커져서 별도 규칙이 필요하면 해당 폴더에 CLAUDE.md를 둘 것.

## 새 실험

`YYYY-MM-주제/` 폴더 + README.md 필수:

    # 주제
    - 질문: 뭘 확인하려는가
    - 상태: 진행중 | 종료
    - 결론: 알아낸 것 (한두 줄)

## 의존성

폴더별로 독립. 루트에 공용 의존성 파일을 만들지 말 것.
Python은 PEP 723 인라인 메타데이터 + `uv run` 우선.